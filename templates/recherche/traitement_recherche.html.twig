{% extends'base.html.twig' %}

{% block body %}

        {{ dump(trajet) }}
        {{ dump(vehicule) }}
        {{ dump(vehicule.membre) }}
    {% for etape in trajet.etapes %}
        {{ dump(etape) }}
        {{ dump(etape.adresse) }}
    {% endfor %}
        {#{{ dump(etape) }}#}
        {#{{ dump(trajet.etape.adresse) }}#}

   {# {% for membre in trajet.membre %}
        {{ dump(membre) }}
    {% endfor %}#}

    {#{% for etape in trajet.etapes %}
        {{ dump(etape) }}
    {% endfor %}#}

{% endblock %}